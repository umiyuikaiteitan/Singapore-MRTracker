[package]
name = "mrt-map-static"
description = "Generate the live schematic train map as a static site for GitHub Pages."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
mrt-gtfs.workspace = true
mrt-gtfs-rt.workspace = true
mrt-datamall.workspace = true
mrt-live = { path = "../mrt-live" }
# The page markup and the snapshot document are shared with the map
# server, so both deployments write the same page.
mrt-map-web = { path = "../mrt-map-web" }
serde.workspace = true
serde_json.workspace = true
