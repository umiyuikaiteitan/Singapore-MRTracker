# Web interface guidance

Read `../docs/STYLE_GUIDE.md` before presentation changes anywhere under `web/`. It covers the hosted OpenFantasyMap editor, live map, and their nested pages and controls. The root `../AGENTS.md` also applies.

`fantasy-map/` is an upstream snapshot: follow `../docs/FANTASY-MAP.md` and its sync procedure; do not hand-edit tracked upstream assets or hashes. Keep this instruction file outside that replaced directory.
