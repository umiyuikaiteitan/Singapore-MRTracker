# Typeface asset

The LTA Identity font data embedded in `../styles.css` is copied from
`umiyuikaiteitan/MRT-RATIS/app/src/main/res/font/lta_identity.ttf` for
this design draft. Embedding keeps the connector-only publication
text-based. The repository owner explicitly approved reuse of the font
for Singapore-MRTracker.
